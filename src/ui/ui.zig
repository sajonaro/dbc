const std = @import("std");

// TODO: ncurses abstractions
pub const Screen = struct {
    // TODO: Implement screen wrapper
};

pub const Window = struct {
    // TODO: Implement window wrapper
};

pub const Rect = struct {
    x: usize,
    y: usize,
    w: usize,
    h: usize,
};
