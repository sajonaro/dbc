const std = @import("std");
const model = @import("../model.zig");
const events = @import("../events.zig");

pub fn handleKey(state: *model.State, key: events.KeyEvent) void {
    // TODO: Implement tree key handling
    _ = state;
    _ = key;
}
