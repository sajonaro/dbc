const std = @import("std");
const view = @import("view.zig");

pub fn render(ctx: view.RenderContext, win: *anyopaque, focused: bool) void {
    // TODO: Implement tree view rendering
    _ = ctx;
    _ = win;
    _ = focused;
}
